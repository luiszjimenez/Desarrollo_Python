##En un codigo Python, las variables se expresan de la siguiente manera
# x = 2 , esto es una variable, constante, donde el valor seguira siendo dos a menos que se cambie por defecto este valor, con algun operador
# c = a + b , eso significa que la sumatoria de a mas b, dara el valor de c... veremos esto ahora con un ejemplo.

x = 2
print(x)
#Esto imprimi en pantalla la variable "x" cuyo valor, es igual a 2, pasemos con el siguiente ejemplo

b = 13
c = x + b
print(c)
#En este ejemplo damos, como resultado, que c es una variable, que resulta de una operacion entre otras dos variables

#Tambien podemos acordar el codigo, para una mejor visualizacion
print(x+b)
#Ojo, en este ejemplo, no asignamos el valor a "c", unicamente imprimimos el valor de dicha operación


#Existen diferentes tipos de datos para las variables, que iremos encontrando en este repositorio